<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;
use App\Models\Home;
use Illuminate\Http\Request;

class HomeTest extends TestCase
{
    /**
     * getHomeDataの「vote」のテストを行う
     */
    public function testGetHomeDataVote(): void
    {
        // $request = new Request;
        // $request->words = '';
        // $request->mode = 'vote';
        // $request->offset = 0;
        // $home = new Home($request);
        // $this->assertTrue(true);
    }
}
